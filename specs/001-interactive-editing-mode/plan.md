# Implementation Plan: Interactive Editing Mode

**Branch**: `001-interactive-editing-mode` | **Date**: 2026-03-26 | **Spec**: `/specs/001-interactive-editing-mode/spec.md`
**Input**: Feature specification from `/specs/001-interactive-editing-mode/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/plan-template.md` for the execution workflow.

## Summary

Build the core interactive editing experience: centered cursor navigation, AI inline question/answer flow, and file context stacking. This is the foundational feature that makes the TUI a living document between human and AI. Success requires smooth rendering (60 FPS, <16ms frame budget) and non-blocking AI integration.

## Technical Context

**Language/Version**: Rust 1.75+
**Primary Dependencies**: Crossterm (terminal I/O), Tokio (async runtime), NEEDS CLARIFICATION: AI integration layer (shard system client)
**Storage**: File I/O (plain text, UTF-8); auto-save to disk
**Testing**: cargo test; manual terminal testing on macOS/Linux/Windows
**Target Platform**: Cross-platform terminal (macOS, Linux, Windows)
**Project Type**: Terminal UI application (TUI)
**Performance Goals**: 60 FPS rendering (16ms frame budget); AI response within 5s (network-dependent); <100ms for UI interactions
**Constraints**: No visual lag or frame drops; input must never be delayed by AI operations; cursor must stay centered
**Scale/Scope**: Single editor instance; support files up to 100MB (with potential degradation)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

**Principle I: Performance Is Non-Negotiable**
- ✅ **PASS**: Success criteria explicitly require 60 FPS and no visual artifacts.
- ✅ **PASS**: Async runtime (Tokio) ensures AI queries don't block rendering.
- ⚠ **VALIDATION**: Rendering path implementation must include frame-time benchmarks; violations trigger rollback.

**Principle II: Collaborative AI-Human Editor First**
- ✅ **PASS**: Core interaction (inline questions, AI responses) is central to this feature.
- ✅ **PASS**: File edit context is preserved across AI interactions.

**Principle III: Stacked Contexts with Code Lens Navigation**
- ✅ **PASS**: User Story 4 (file switching) implements the navigation stack directly.
- ✅ **PASS**: Context (cursor position, viewport) is saved/restored on file close/return.

**Principle IV: Shard-Aware Integration**
- ⚠ **CLARIFICATION NEEDED**: Shard system client library and API contract must be defined (Phase 0 research).
- ⚠ **GATE**: AI integration in P1 user stories requires shard client availability.

**Principle V: Terminal-Native and Simplicity-First**
- ✅ **PASS**: Pure TUI, no GUI.
- ✅ **PASS**: Feature scope excludes syntax highlighting, plugins, and mouse (defer to later).

**Constitution Compliance Summary**: PASS (with P0 research required for shard integration details)

## Project Structure

### Documentation (this feature)

```text
specs/001-interactive-editing-mode/
├── plan.md              # This file
├── research.md          # Phase 0: Shard integration, Rust terminal libraries, AI protocol
├── data-model.md        # Phase 1: Editor state, file model, context stack
├── quickstart.md        # Phase 1: Dev setup, running the TUI, basic workflow
├── contracts/           # Phase 1: AI question/response schema
│   └── ai-interaction.md
└── tasks.md             # Phase 2: Implementation tasks (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
# Single Rust project structure (Option 1: DEFAULT)
src/
├── main.rs              # Entry point
├── ui/
│   ├── mod.rs
│   ├── renderer.rs      # Frame rendering, cursor centering
│   ├── viewport.rs      # Viewport scrolling logic
│   └── input.rs         # Keyboard input handling
├── editor/
│   ├── mod.rs
│   ├── buffer.rs        # File buffer, line management
│   ├── cursor.rs        # Cursor position tracking
│   └── stack.rs         # Context stack for file navigation
├── ai/
│   ├── mod.rs
│   ├── client.rs        # Shard system client (NEEDS CLARIFICATION)
│   └── interaction.rs   # Question/response handling
├── file/
│   ├── mod.rs
│   └── io.rs            # File I/O, auto-save
└── lib.rs               # Library exports

tests/
├── integration/
│   ├── rendering_test.rs
│   ├── navigation_test.rs
│   └── ai_interaction_test.rs
├── unit/
│   ├── cursor_test.rs
│   ├── buffer_test.rs
│   └── stack_test.rs
└── benches/
    └── rendering_bench.rs # Performance benchmarks (required by constitution)
```

**Structure Decision**: Single Rust project with modular organization by responsibility (UI, editor, AI, file I/O). This keeps the codebase focused and aligns with the simplicity-first principle. Terminal testing will be manual (macOS/Linux/Windows); integration tests use virtual file fixtures.

## Complexity Tracking

No Constitution violations requiring justification at this stage. All complexity is necessary:
- Async runtime (Tokio) is required to prevent AI calls from blocking rendering.
- Context stack is required by Principle III (stacked contexts).
- Separate modules are necessary for clarity given the distinct responsibilities (rendering, editing, AI, file I/O).

---

## Next Phase: Phase 0 Research

**Blockers to resolve before Phase 1 design**:
1. Shard system client API and integration requirements (who provides the AI endpoint? What protocol?)
2. Rust terminal library evaluation (Crossterm vs. Termion vs. alternatives; cross-platform support)
3. Performance baseline: what frame time is achievable on typical hardware with Crossterm?
4. File auto-save strategy (on every keystroke? Debounced? Explicit save?)

**Phase 0 output**: `/specs/001-interactive-editing-mode/research.md` will resolve these before proceeding to Phase 1 design.
