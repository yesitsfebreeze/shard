{
  "mcpServers": {
    "shard": {
      "command": "bash",
      "args": ["-c", "docker compose -f \"$PWD/docker-compose.yml\" down -v >/dev/null 2>&1; docker compose -f \"$PWD/docker-compose.yml\" up --build -d >/dev/null 2>&1; exec docker exec -i shard /shard --mcp"]
    }
  }
}
