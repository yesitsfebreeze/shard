services:
  shard:
    build: .
    container_name: shard
    ports:
      - "7777:8080"
      - "3333:3333"
    volumes:
      - ./.shards:/root/.shards
      - ./app:/app/web
      - /app/web/node_modules
    env_file:
      - .env
    restart: unless-stopped
